SSH_PORT=6922
HTTP_PORT=8080
DATA_DIR=./data

# Logto OAuth — set all five vars to enable browser-based identity binding.
# Leave LOGTO_ENDPOINT blank to use SSH-key-based nick registration instead.
LOGTO_ENDPOINT=
LOGTO_APP_ID=
LOGTO_APP_SECRET=
LOGTO_REDIRECT_URI=
HTTP_LISTEN_ADDR=0.0.0.0:8080
